JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
PORT=5001
NODE_ENV=development

# MongoDB Connection URI
# For local development:
# MONGODB_URI=mongodb://localhost:27017/didueat

# For Railway (add this in Railway environment variables):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/didueat?retryWrites=true&w=majority

# Or if using Railway's MongoDB plugin:
# MONGODB_URI=${{MONGO_URL}}
